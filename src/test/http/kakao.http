### 환경 변수
@baseUrl = http://localhost:8080

### 1. 카카오 로그인 페이지 접속 (브라우저에서 실행)
# https://kauth.kakao.com/oauth/authorize?client_id={{kakao_client_id}}&redirect_uri={{redirect_uri}}&response_type=code

### 2. 카카오 인증 콜백 (로그인 후 받은 코드로 JWT 토큰 발급)
GET {{baseUrl}}/api/auth/kakao/callback?code={{code}}

> {%
    client.global.set("jwt_token", response.body.data.accessToken);
%}

### 3. 보호된 리소스 접근 (Access Token 사용)
GET {{baseUrl}}/api/test/protected
Content-Type: application/json
Authorization: Bearer {{access_token}}

### 4. 토큰 갱신 (Access Token 만료 시)
# Redis에 저장된 Refresh Token을 사용하여 새로운 Access Token 발급
POST {{baseUrl}}/api/auth/kakao/refresh
Content-Type: application/json
Authorization: Bearer {{expired_access_token}}